{
  "name": "implo-launcher",
  "version": "1.0.0",
  "main": "electron/bundle.js",
  "scripts": {
    "init": "npm run angular:watch",
    "start": "npm run electron:dev",
    "clean": "rm -rf release && rm -rf dist",
    "angular:watch": "(cd angular && npm run watch)",
    "electron:dev": "(cd electron && npm run dev)",
    "package:mac": "npm run clean && (cd angular && npm run build) && (cd electron && npm run package:mac)",
    "package:win": "npm run clean && (cd angular && npm run build && cd ..) && (cd electron && npm run package:win && cd ..)"
  },
  "devDependencies": {
    "electron-packager": "^15.2.0",
    "typescript": "^4.3.2",
    "electron": "^13.1.8"
  }
}
